# Harness skills registry

Skills are reusable capability packets attached by the planner after clarified need, not mapped from founder keywords.

Source: `src/orchestration/harnessSkillsRegistry.js`, `harnessSkillBindings.js`.

Test: `npm test` runs `scripts/test-vnext13-2-skills-registry.mjs`.
