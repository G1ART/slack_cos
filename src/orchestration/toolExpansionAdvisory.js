/**
 * vNext.13.2 — Tool/connector/subscription gaps (advisory copy for COS governance; not a router).
 */

export function describeConnectorAndSubscriptionAngle() {
  return (
    '발송·템플릿·동의 로그를 남기려면 문서·메일·CRM 커넥터와 구독 범위를 명시하고, ' +
    '승인 전에는 외부 발송이 나가지 않도록 게이트를 분리하는 구성이 일반적으로 필요합니다.'
  );
}

export function describeReviewAndGuardianPattern() {
  return '추가 reviewer(예: 컴플라이언스·편집)나 governance guardian을 두는 것도 선택지입니다.';
}
