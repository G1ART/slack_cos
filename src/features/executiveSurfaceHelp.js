/**
 * North Star Fast-Track v1 — 대표 표면 도움말만 (명령 미로 노출 금지).
 * 전체 실행 어휘는 `운영도움말` + `operatorHelpText` (app.js).
 * @see docs/cursor-handoffs/COS_FastTrack_v1_Surface_And_Routing.md
 */

export function formatExecutiveHelpText() {
  return [
    '━━ COS · North Star Fast-Track (대표 표면) ━━',
    '**역할** — 당신은 **대표**, COS는 **비서실장(단일 창구)**. 페르소나·R&R이 나뉜 다른 슬랙 에이전트와는 구분됩니다.',
    '**GOAL-IN / DECISION-OUT** — 목표·결정은 말로, 엔진 조작 문법은 외우지 않아도 됩니다.',
    '**큰 그림** — 시중 **harness·멀티페르소나 에이전트** 생산성을 **슬랙·COS 단일 창구**로 옮겨, 인간 병목 없이 같은 허브에서 실행·감사·확장(툴/앱/그랜트·IR 등)까지 가는 **워크스페이스**를 쌓는다. (`docs/cursor-handoffs/COS_Workspace_Vision_CompanyScale_2026-03.md`)',
    '',
    '**대표가 쓰는 표면 (5+α)** — 나머지 문자열 명령은 *운영/에이전트용* (`운영도움말`)',
    '1. **시작** — `프로젝트시작: …` · `툴시작: …` · `툴제작: …` (목표 한 줄 → 실행 큐 적재 → `실행큐계획화 CWS-…` 한 줄이면 PLN·WRK)',
    '   _로컬·스테이징에서 빠르게 보려면:_ 봇 프로세스에 `COS_FAST_SPEC_PROMOTE=1` — 같은 메시지에서 PLN·WRK까지 생성 (운영 기본값은 끔).',
    '2. **상태** — `지금 상태 보여줘` · `현재 상태` 등 (짧은 상태 패킷)',
    '3. **의사결정** — `결정비교: …` → 패킷; 같은 스레드에서 `1안`·`보류` 등 짧은 답',
    '4. **배포·준비** — `배포 준비` 등 — *승인 매트릭스 formalize 예정*',
    '5. **중단·보류** — `이건 보류` 등',
    '6. **전략·리스크(얇은 인입)** — `전략 검토:` · `리스크 검토:` (다각은 `협의모드:`)',
    '7. **피드백 (배포 후 루프)** — `피드백: …` · `제품 피드백: …` — `customer_feedback` 큐(`data/cos-workspace-queue.json`)에 traceable 저장',
    '',
    '**자연어** — 평문으로 지시·토론하면 비서실장 COS가 되물으며 정렬합니다. `COS …` / `비서 …` 내비.',
    '**다각 논의(옵트인)** — `협의모드: …` (Council·장문은 기본이 아님)',
    '**조회만 빠르게** — 한 줄 `계획상세 PLN-…` 등 또는 채널 `/g1cos …`',
    '**감사·드릴다운(M4)** — `상태 STP-…` · `패킷 PKT-…` · `턴 <uuid>` · **실행/피드백 큐:** `실행 큐 CWS-…` · `고객 피드백 CFB-…` · 목록 `실행 큐 목록` · `고객 피드백 목록` (멘션·`/g1cos` 동일).',
    '**실행 큐 → 코딩** — `실행큐계획화 최근` → `커서발행 WRK-…` (핸드오프 마크다운에 PLN·CWS 링크 포함).',
    '',
    '_내부 실행 어휘·디버그 목록:_ `운영도움말`',
  ].join('\n');
}
